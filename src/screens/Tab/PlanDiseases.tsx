/** @format */

import React from "react";
import { ScrollView, Text } from "native-base";

const PlanDiseases = () => {
	return (
		<ScrollView
			_dark={{
				bg: "dark.200",
			}}>
			<Text>PlanDiseases</Text>
		</ScrollView>
	);
};

export default PlanDiseases;
